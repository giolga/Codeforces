#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n, i;
    cin >> n;

    for(i = 0; i < n; i++) { 
        long long a;
        cin >> a;
        cout << (a - 1) / 2 << endl;
    }
    return 0;
}
